{
  "scripts": {
    "start": "elm-live src/Results.elm --start-page=dev.html --open -- --output=dev.js --debug",
    "current": "elm-live src/CurrentGames.elm --start-page=current-games-dev.html --open -- --output=current-games-dev.js --debug"
  },
  "devDependencies": {
    "elm": "^0.19.1-5",
    "elm-live": "^4.0.2"
  }
}
